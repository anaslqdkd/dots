require("oil").setup({
	-- prompt_save_on_select_new_entry = false,
	-- delete_to_trash = true,
	-- skip_confirm_for_simple_edits = false,
	keymaps = {
		-- You can override or disable Oil's default mappings by setting them to `false`
		["<C-h>"] = false,
		["<C-l>"] = false,
	},
})
